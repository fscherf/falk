{
  "scripts": {
    "_install": "test -d node_modules || npm install",
    "build": "npm run _install && webpack build",
    "watch": "npm run _install && webpack watch --mode=development",
    "lint": "npm run _install && prettier --write ./client"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "morphdom": "^2.7.8",
    "prettier": "^3.8.3",
    "ts-loader": "^9.5.7",
    "webpack": "^5.106.2",
    "webpack-cli": "^7.0.2",
    "npm-check-updates": "^22.1.0"
  }
}
